# Useful paths in Kubernetes cluster nodes

## Control plane
1. CNI
    - `/etc/cni/net.d/`
    - `/opt/cni/bin/`
    - `/etc/cni/net.d/10-weave.conflist`

2. ETCD
    - `/etc/kubernetes/manifests/etcd.yaml`